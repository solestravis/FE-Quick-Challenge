import styled from 'styled-components';

export const StyledListItem = styled.li`
    font-size: 1.5rem;
    padding: 0.7rem 0;
    text-transform: uppercase;
    
    &:hover {
        background: #e8e8e8;
        color: #d9412b;
        cursor: pointer;
    }
`;
